# Backlog

Items not yet scheduled. Add new work here. Move to `todo.md`
when it becomes active.

<!-- Newest items at the top. -->

- **Bash policy hook** — PreToolUse hook that refuses or forces a prompt
  on ruled-out command shapes, rules in a ledger-style policy file;
  replaces six dbb memory entries once live. Plan:
  `plans/bash-policy-hook.md`. Pairs with dbb DBB-0547 (read-only
  development connection). Added 2026-09-16.
